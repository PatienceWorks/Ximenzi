# CIMC 题目合作开发方案

## 1. 合作原则

这道题不建议所有人一起改同一批文件，容易出现代码冲突，也不方便排查问题。

推荐方式是：

```text
按模块分工
统一函数接口
每个人在自己的分支开发
通过 Pull Request 合并到 main
```

其中 `main` 分支只保存已经测试通过的稳定代码。

## 2. 工程分层

建议保持当前工程结构：

```text
HardWare      外设驱动层
System        系统基础模块
Protocol      通信协议层
sysFunction   比赛题目逻辑层
Function      系统初始化和主循环入口
HeaderFiles   总头文件管理
```

需要特别注意：题目要求所有逻辑层程序功能都放在 `sysFunction` 文件夹下，否则可能扣分。

所以：

```text
驱动代码放 HardWare / System
题目业务逻辑放 sysFunction
```

不要把比赛逻辑直接写进 `usart.c`、`ADC.c`、`RTC.c` 这些驱动文件里。

## 3. 推荐分工

### 3.1 底层驱动负责人

负责移植和测试外设驱动：

```text
USART
LED
KEY
OLED
RTC
ADC
SPI Flash
SD Card / FatFs
```

目标是保证每个外设能单独工作，并提供简单接口，例如：

```c
void LED_Init(void);
void KEY_Init(void);
void usart_send_string(const char *str);
float ADC_GetVoltage(void);
void RTC_GetTime(void);
```

底层驱动负责人只负责“外设能不能用”，不负责完整题目逻辑。

### 3.2 串口命令负责人

负责串口命令解析，主要处理：

```text
test
RTC Config
RTC now
conf
ratio
limit
config save
config read
start
stop
hide
unhide
```

这部分代码应该放在：

```text
sysFunction
```

串口命令负责人不直接操作 ADC、Flash、RTC 的底层细节，而是调用统一接口。

### 3.3 采样控制负责人

负责采样状态机：

```text
start 启动采样
stop 停止采样
KEY1 切换采样状态
KEY2 / KEY3 / KEY4 切换采样周期 5s / 10s / 15s
采样时 LED1 以 1s 周期闪烁
停止采样时 LED1 常灭
超限时 LED2 点亮
```

这部分代码也应放在：

```text
sysFunction
```

### 3.4 数据处理负责人

负责电压数据计算和编码处理：

```text
ADC 原始值转电压
ratio 变比计算
limit 阈值判断
OverLimit 标志
hide 编码
unhide 解码
Unix 时间戳转换
```

题目采集的是 `Ch0` 电压值，基本流程是：

```text
ADC 采集模拟电压
换算成实际输入电压
乘以 ratio 变比
得到 Ch0 电压值
```

### 3.5 存储负责人

负责参数和文件相关功能：

```text
从 TF 卡读取 config.ini
读取 Ch0 的 ratio 和 limit
保存配置到外部 Flash
从外部 Flash 读取配置
采样周期掉电保存
```

重点测试：

```text
TF 卡不存在
config.ini 不存在
Flash 读写是否正常
掉电重启后配置是否恢复
```

### 3.6 OLED 显示负责人

负责 OLED 显示内容：

停止采样时：

```text
第一行：system idle
第二行：空
```

采样状态下：

```text
第一行：hh:mm:ss
第二行：xx.xx V
```

OLED 显示应和串口输出的数据保持一致。

## 4. 统一接口设计

合作前最好先约定一批函数接口，避免每个人乱起函数名。

建议在 `sysFunction` 里建立类似文件：

```text
sys_config.c / sys_config.h
sys_command.c / sys_command.h
sys_sample.c / sys_sample.h
sys_data.c / sys_data.h
sys_storage.c / sys_storage.h
sys_display.c / sys_display.h
```

可以约定这些接口：

```c
void Sys_Init(void);

void Sys_StartSample(void);
void Sys_StopSample(void);
void Sys_ToggleSample(void);
uint8_t Sys_IsSampling(void);

void Sys_SetSamplePeriod(uint16_t period_s);
uint16_t Sys_GetSamplePeriod(void);

float Sys_GetVoltage(void);
uint8_t Sys_IsOverLimit(float voltage);

void Sys_SetRatio(float ratio);
float Sys_GetRatio(void);

void Sys_SetLimit(float limit);
float Sys_GetLimit(void);

void Sys_SaveConfig(void);
void Sys_LoadConfig(void);

void Sys_CommandProcess(const char *cmd);
void Sys_DisplayIdle(void);
void Sys_DisplaySample(float voltage);
```

这样别人写串口命令时，只需要调用接口，不需要知道底层怎么实现。

## 5. Git 合作流程

### 5.1 main 分支

`main` 分支只放稳定版本。

不要直接在 `main` 上乱改。每个人开发新功能时，从 `main` 拉一个自己的分支。

### 5.2 每个人建立自己的功能分支

示例：

```text
feature/usart-command
feature/adc-sample
feature/rtc-oled
feature/storage
feature/key-led
```

命名建议：

```text
feature/功能名
fix/问题名
```

例如：

```text
feature/command-parser
feature/flash-config
fix/usart-receive-bug
```

### 5.3 开发前先同步 main

每次开始写代码前，先更新本地代码：

```bash
git checkout main
git pull
```

然后创建或切换到自己的分支：

```bash
git checkout -b feature/adc-sample
```

如果分支已经存在：

```bash
git checkout feature/adc-sample
```

### 5.4 提交代码

改完一个小功能就提交一次。

查看改动：

```bash
git status
```

添加文件：

```bash
git add 文件名
```

提交：

```bash
git commit -m "Add ADC sample test"
```

提交信息建议写清楚做了什么，例如：

```text
Add RTC command handler
Fix USART receive buffer overflow
Add flash config save and read
```

### 5.5 推送到 GitHub

```bash
git push origin feature/adc-sample
```

然后在 GitHub 网页上创建 Pull Request。

### 5.6 代码审查

Pull Request 中重点检查：

```text
有没有改到别人的模块
有没有把题目逻辑写进驱动层
有没有破坏已有功能
有没有忘记添加 .c 文件
有没有忘记添加头文件路径
有没有明显的死循环或阻塞延时
```

审查通过后，再合并到 `main`。

## 6. 集成顺序

建议按下面顺序集成，不要一次性把所有功能合进去。

### 第一步：基础外设

```text
USART
LED
KEY
```

验收标准：

```text
串口能收发
LED 能亮灭
按键能触发串口打印或 LED 翻转
```

### 第二步：核心采样

```text
ADC
ratio
limit
OverLimit
```

验收标准：

```text
串口能打印 Ch0 电压值
ratio 修改后电压计算变化
超过 limit 后 LED2 点亮
```

### 第三步：时间和显示

```text
RTC
OLED
```

验收标准：

```text
RTC Config 可以设置时间
RTC now 可以读取时间
OLED 停止状态显示 system idle
OLED 采样状态显示时间和电压
```

### 第四步：配置和存储

```text
TF 卡
config.ini
外部 Flash
```

验收标准：

```text
conf 能读取配置文件
config save 能保存参数
config read 能读出参数
掉电重启后参数还在
```

### 第五步：完整命令

```text
test
start
stop
hide
unhide
```

验收标准：

```text
所有题目要求命令都能按格式输出
采样启停正常
隐藏和解译结果正确
```

## 7. 文件修改边界

为了减少冲突，每个人只改自己负责的文件。

推荐边界：

```text
驱动负责人：HardWare / System
命令负责人：sysFunction/sys_command.*
采样负责人：sysFunction/sys_sample.*
数据负责人：sysFunction/sys_data.*
存储负责人：sysFunction/sys_storage.*
显示负责人：sysFunction/sys_display.*
```

公共文件例如：

```text
HeaderFiles.h
Function.c
Keil 工程文件 .uvprojx
```

这些文件容易冲突，修改前最好先和队友说一声。

## 8. 每次合并前的检查清单

合并到 `main` 前，至少检查：

```text
Keil Rebuild 是否 0 Error
串口是否还能正常打印
已有命令是否还能用
是否新增了不必要的大文件
是否把临时测试代码删掉
是否符合题目要求的 sysFunction 目录规则
```

如果是外设相关改动，还要上板测试。

## 9. 推荐每日协作方式

每天开始前：

```text
同步 main
说明今天各自做什么模块
确认是否会修改公共文件
```

每天结束前：

```text
提交当天代码
推送到自己的分支
写清楚已完成什么、还差什么、怎么测试的
```

## 10. 最重要的注意事项

这道题真正容易乱的地方不是 ADC、RTC 这些单个外设，而是最后所有功能混在一起。

所以一定要坚持：

```text
驱动层只做驱动
sysFunction 只做题目逻辑
main 只放稳定代码
公共接口先约定再开发
每次只合并一个可测试的小功能
```

这样合作起来会清晰很多，后期也更容易查问题。
