# 引脚说明

本文档记录当前工程已经使用和验证过的外设引脚，方便接线、调试和队友协作。

## 主控

- MCU：GD32F470
- 工程：`01 CIMC_GD32_Template`

## USART0 串口

用途：串口命令输入、自检结果输出、采样数据输出。

| 功能 | MCU 引脚 | 说明 |
| --- | --- | --- |
| USART0_TX | PA9 | 单片机发送，接 USB 转串口 RX |
| USART0_RX | PA10 | 单片机接收，接 USB 转串口 TX |

串口参数：

```text
115200, 8N1
```

## LED

用途：状态指示、采样指示、超限报警。

| LED | MCU 引脚 | 当前用途 |
| --- | --- | --- |
| LED1 | PA4 | 采样运行时 1 秒间隔闪烁 |
| LED2 | PA5 | Ch0 超过 limit 时点亮 |
| LED3 | PA6 | 备用 |
| LED4 | PA7 | 备用 |

## KEY

用途：按键控制采样流程和采样周期。

| 按键 | MCU 引脚 | 当前用途 |
| --- | --- | --- |
| KEY1 | PE6 | 开始/停止采样 |
| KEY2 | PE3 | 设置采样周期为 5s |
| KEY3 | PE4 | 设置采样周期为 10s |
| KEY4 | PE5 | 设置采样周期为 15s |

注意：

- 原先 KEY1 使用 `PE2`，但实测 `PE2` 一直被拉低，导致程序认为 KEY1 一直按下。
- 当前工程已经把 KEY1 改为 `PE6`。
- 按键输入使用上拉方式，低电平有效；`KEY_Stat()` 返回 `1` 表示按下，返回 `0` 表示未按下。

## OLED

用途：显示系统空闲状态、RTC 时间、Ch0 电压。

| 功能 | MCU 引脚 | 说明 |
| --- | --- | --- |
| OLED_SCL | PB8 | 模拟 I2C 时钟 |
| OLED_SDA | PB9 | 模拟 I2C 数据 |

## ADC

用途：采集输入电压，并通过 `ratio` 换算为 Ch0 电压。

| 功能 | MCU 引脚 | ADC 通道 |
| --- | --- | --- |
| ADC 输入 | PC0 | ADC_CHANNEL_10 |

说明：

- 当前 ADC 基准按 `VDDA = 3.3V` 计算。
- 串口输出中的 `adc` 是 PC0 实测电压。
- 串口输出中的 `ch0` 是 `adc * ratio` 后的结果。

## SPI Flash

用途：保存配置参数，实现掉电保存。

| 功能 | MCU 引脚 |
| --- | --- |
| SPI0_CLK | PB3 |
| SPI0_MISO | PB4 |
| SPI0_MOSI | PB5 |
| SPI0_CS | PA15 |

当前已验证：

```text
Flash ID: 0xC84013
```

## TF Card / SDIO

用途：读取 TF 卡根目录下的 `config.ini` 配置文件。

| 功能 | MCU 引脚 |
| --- | --- |
| SDIO_DAT0 | PC8 |
| SDIO_DAT1 | PC9 |
| SDIO_DAT2 | PC10 |
| SDIO_DAT3 | PC11 |
| SDIO_CLK | PC12 |
| SDIO_CMD | PD2 |

配置文件路径：

```text
config.ini
```

配置文件内容示例：

```ini
ratio=1.0
limit=100.0
period=5
```

## 常用串口命令

| 命令 | 作用 |
| --- | --- |
| `test` | 系统自检，检测 Flash、TF 卡、RTC |
| `config read` | 从 TF 卡读取 `config.ini` |
| `config save` | 保存当前配置到外部 Flash |
| `config load` | 从外部 Flash 恢复配置 |
| `start` | 开始采样 |
| `stop` | 停止采样 |
| `key test` | 打印 KEY1~KEY4 原始电平和按下状态 |
| `RTC now` | 打印当前 RTC 时间 |
| `RTC Config` | 设置 RTC 时间 |
| `ratio` | 修改 ratio |
| `limit` | 修改 limit |

