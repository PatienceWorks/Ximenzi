# 2025 CIMC 工业嵌入式初赛开发顺序

## 0. 赛题结论

本题不是单独外设驱动题，而是一个完整的数据采集系统。核心目标是：

```text
串口命令交互
    ↓
RTC 时间管理
    ↓
ADC 电压采集
    ↓
比例变比 / 阈值处理
    ↓
OLED 显示
    ↓
LED / KEY 状态控制
    ↓
外部 Flash 参数保存
    ↓
TF 卡文件存储
    ↓
日志审计和隐藏数据编码
```

赛题明确要求：**所有逻辑层程序功能都需要放在 `sysFunction` 文件夹下，否则总分按 -20 分处理。**

因此第一步不是马上写功能，而是先整理工程架构。

## 1. 推荐工程目录

建议把工程分成下面几层：

```text
User/                  main、systick、中断
HeaderFiles/           统一头文件入口
HardWare/              单个硬件驱动
  LED/
  KEY/
  ADC/
  OLED/
  RTC/
  Flash/
  TFCard/
  USART/
sysFunction/           赛题业务逻辑，必须建立
  sys_init.c/h         上电初始化
  sys_cmd.c/h          串口命令解析
  sys_config.c/h       ratio、limit、config save/read/conf
  sys_sample.c/h       采样状态机
  sys_storage.c/h      sample、overLimit、hideData、log 文件管理
  sys_hide.c/h         hide/unhide 编码
  sys_selftest.c/h     test 自检
  sys_log.c/h          操作审计日志
project/               Keil 工程文件与编译产物
```

`Function` 目录可以保留，但核心赛题逻辑应迁到 `sysFunction`。`Function.c` 最好只保留很薄的一层调用：

```c
System_Init();
SysFunction_Run();
```

## 2. 总开发原则

优先开发顺序按“底层依赖先行、评分流程优先、可逐项演示”安排。

不要一开始就做 TF 卡复杂存储。先让系统能稳定：

```text
能启动
能串口收命令
能显示
能采样
能保存参数
能写文件
最后再做日志和隐藏编码
```

每完成一个阶段，都要在 Keil 中：

```text
Rebuild
Download
串口验证
记录当前可用版本
```

## 3. 第 1 阶段：工程基础与上电初始化

目标：系统上电后能输出初始化信息，并进入空闲状态。

需要完成：

```text
1. 建立 sysFunction 文件夹
2. 新建 sys_init.c/h
3. 初始化 USART
4. 初始化 OLED
5. 初始化 LED
6. 初始化 KEY
7. 初始化 ADC
8. 初始化 RTC
9. 初始化 Flash
10. 初始化 TF 卡挂载接口
```

上电串口输出应满足评分流程：

```text
====system init====
Device_ID:2025-CIMC-队伍编号
====system ready====
```

OLED 上电后显示：

```text
system idle
```

验收点：

```text
串口能打印初始化信息
OLED 能显示 system idle
LED 默认熄灭
系统主循环不死机
```

## 4. 第 2 阶段：串口命令框架

目标：所有功能都通过统一命令入口调度。

先实现命令识别，不急着实现完整功能：

```text
test
RTC Config
RTC now
conf
ratio
limit
config save
config read
start
stop
hide
unhide
```

建议实现一个命令表：

```c
typedef void (*cmd_handler_t)(char *args);

typedef struct {
    const char *name;
    cmd_handler_t handler;
} sys_cmd_t;
```

验收点：

```text
串口输入不同命令，能进入对应 handler
未知命令返回 Unknown command
命令处理不阻塞采样状态机
```

## 5. 第 3 阶段：RTC 时间功能

目标：完成评分流程第 2 项。

实现命令：

```text
RTC Config
RTC now
```

`RTC Config` 流程：

```text
输入 RTC Config
返回 Input Datetime
用户输入 2025-01-01 15:00:10
解析年月日时分秒
写入 RTC
返回设置结果
```

`RTC now` 流程：

```text
读取 RTC 当前时间
通过串口打印
```

注意：

```text
题目允许日期分隔符不固定
可以兼容 2025-01-01 15:00:10
也可以兼容 2025年01月01日15:00:10
```

验收点：

```text
RTC Config 能设置时间
RTC now 能读取当前时间
OLED 后续可以使用 RTC 的 hh:mm:ss
```

## 6. 第 4 阶段：参数模型与外部 Flash

目标：先把系统参数统一管理，后续采样、超限、存储都依赖它。

参数结构建议：

```c
typedef struct {
    float ratio;
    float limit;
    uint8_t sample_period_s;
    uint32_t boot_count;
    char device_id[32];
} sys_config_t;
```

默认值建议：

```text
ratio = 1.0
limit = 30.0
sample_period_s = 5
device_id = 2025-CIMC-队伍编号
```

实现命令：

```text
ratio
limit
config save
config read
```

输入范围按赛题：

```text
ratio 有效范围：0 - 100，float
limit 赛题前文写 0 - 500，评分流程写 0 - 200
```

建议按评分流程实现：

```text
limit 有效范围：0 - 200
```

原因：评分流程明确会测试 `limit=300` 为非法。

验收点：

```text
ratio=1.0 能保存
ratio=199.99 返回参数无效
limit=30 能保存
limit=300 返回参数无效
config save 能写入 Flash
断电后 config read 能读回 ratio
```

## 7. 第 5 阶段：ADC 电压采集

目标：采集滑动变阻器电压，得到可显示、可计算的电压值。

需要完成：

```text
1. 确认原理图中电位器 ADC 通道
2. 初始化对应 GPIO 为模拟输入
3. 初始化 ADC
4. 完成单次采样函数
5. 将 ADC 原始值转换为电压
6. 应用 ratio 变比
```

数据模型建议：

```c
typedef struct {
    rtc_time_t time;
    float raw_voltage;
    float value;
    uint8_t over_limit;
} sample_data_t;
```

计算关系：

```text
raw_voltage = adc_value / 4095.0 * 3.3
value = raw_voltage * ratio
```

验收点：

```text
转动电位器，串口调试输出电压变化
ratio=10 后，显示值约放大 10 倍
```

## 8. 第 6 阶段：采样状态机

目标：实现 start/stop 和按键启停。

系统状态建议：

```c
typedef enum {
    SYS_IDLE = 0,
    SYS_SAMPLING
} sys_state_t;
```

实现命令：

```text
start
stop
```

实现按键：

```text
KEY1：启动/停止采样状态翻转
KEY2：采样周期 5s
KEY3：采样周期 10s
KEY4：采样周期 15s
```

采样时：

```text
LED1 按 1s 周期闪烁
OLED 第一行显示 hh:mm:ss
OLED 第二行显示 xx.xx V
串口按采样周期输出数据
```

停止时：

```text
LED1 常灭
OLED 第一行显示 system idle
OLED 第二行为空
```

验收点：

```text
start 后开始采样
stop 后停止采样
KEY1 可以切换采样状态
KEY2/KEY3/KEY4 可以改变采样周期
周期断电后仍然生效
```

## 9. 第 7 阶段：OLED 显示

目标：按赛题要求显示系统状态。

空闲状态：

```text
第一行：system idle
第二行：空
```

采样状态：

```text
第一行：hh:mm:ss
第二行：xx.xx V
```

注意：

```text
除了采集状态下 OLED 刷新数据，其余时刻都显示 system idle
```

验收点：

```text
上电显示 system idle
start 后显示时间和电压
stop 后恢复 system idle
```

## 10. 第 8 阶段：超限判断

目标：采样值超过 limit 时点亮 LED2 并输出 OverLimit。

逻辑：

```text
if value > limit:
    over_limit = 1
    LED2_ON()
    串口输出增加 OverLimit 和 limit
else:
    over_limit = 0
    LED2_OFF()
```

注意：

```text
评分流程会设置 ratio=10
然后把电位器调到 3V 以上
此时 value > 30，应触发超限
```

验收点：

```text
超过阈值时 LED2 点亮
低于阈值时 LED2 熄灭
串口格式符合题目截图要求
```

## 11. 第 9 阶段：TF 卡与文件系统

目标：完成所有文件夹和数据文件存储。

上电后需要创建或检查：

```text
/sample
/overLimit
/log
/hideData
```

采样数据：

```text
/sample/sampleDataYYYYMMDDhhmmss.txt
每个文件 10 条数据
超过 10 条新建文件
```

超限数据：

```text
/overLimit/overLimitYYYYMMDDhhmmss.txt
每个文件 10 条数据
超过 10 条新建文件
```

隐藏数据：

```text
/hideData/hideDataYYYYMMDDhhmmss.txt
每个文件 10 条数据
超过 10 条新建文件
```

日志文件：

```text
/log/log{id}.txt
每次上电新建一个文件
id 从 Flash 中读取 boot_count
TF 卡清空后 id 也不能从 0 重来
```

验收点：

```text
采集 20 条以上，sample 至少生成多个文件或写入超过 10 条滚动
超限 20 条以上，overLimit 正确生成文件
断电拔卡后电脑能看到文件
```

## 12. 第 10 阶段：配置文件读取 conf

目标：从 TF 卡读取 `config.ini`，更新 ratio 和 limit 到 Flash。

命令：

```text
conf
```

如果文件不存在：

```text
返回“无此文件”
```

如果文件存在：

```text
读取 Ch0 的 ratio 和 limit
更新系统参数
写入外部 Flash
串口打印读取结果
```

配置文件格式按题目示例，只读取 Ch0。

验收点：

```text
TF 卡根目录无 config.ini 时，返回无此文件
存在 config.ini 时，能读取 Ch0 参数
读取后 config read 能显示新参数
```

## 13. 第 11 阶段：hide/unhide 数据编码

目标：实现串口输出和存储格式切换。

命令：

```text
hide
unhide
```

hide 后输出编码：

```text
时间戳：4 字节 Unix timestamp，HEX
电压值：4 字节
  整数部分：2 字节，高位在前
  小数部分：2 字节，高位在前
```

示例：

```text
2025-01-01 12:30:45 ch0=12.5V
timestamp = 1735705845 = 6774C4F5
整数 12 = 000C
小数 0.5 * 65536 = 8000
最终：6774C4F5000C8000
```

如果发生超限：

```text
编码末尾增加 *
```

存储规则：

```text
hide 启用后，sample 文件夹不存储普通采样数据
hideData 文件夹存储隐藏数据
如果超限，overLimit 仍然按普通格式存储
```

验收点：

```text
hide 后串口输出 HEX 编码
unhide 后恢复普通输出
超限隐藏数据末尾有 *
hideData 文件可被校验工具验证
```

## 14. 第 12 阶段：操作审计日志

目标：按评分流程记录操作和结果。

需要记录的典型操作：

```text
RTC Config
RTC now
test
ratio
limit
start
stop
KEY1
KEY2
KEY3
KEY4
config save
config read
hide
unhide
conf
```

日志文件：

```text
/log/log0.txt
/log/log1.txt
/log/log2.txt
...
```

每次上电新建一个日志文件，id 由 Flash 中的上电次数决定。

建议每条日志格式：

```text
[YYYY-MM-DD hh:mm:ss] CMD=start RESULT=OK
[YYYY-MM-DD hh:mm:ss] KEY=KEY3 RESULT=period=10s
```

验收点：

```text
评分流程中要求的操作能在对应 log 文件中找到
TF 卡清空后，log id 仍然继续增长
```

## 15. 第 13 阶段：系统自检 test

目标：实现系统自检。

命令：

```text
test
```

需要检查：

```text
Flash：能读取 ID 即通过
TF card：能检测到卡存在即通过
OLED：初始化状态
RTC：初始化状态
ADC：初始化状态
LED/KEY：初始化状态
```

评分流程要求：

```text
第一次不插 TF 卡，test 显示 TF card 不通过
插卡重新上电，test 显示所有状态 ok
```

验收点：

```text
无 TF 卡时能明确提示 TF card fail
有 TF 卡时能提示 TF card ok
Flash ID 可读取
```

## 16. 推荐实际开发顺序总表

| 顺序 | 模块 | 原因 | 验收方式 |
|---|---|---|---|
| 1 | 工程架构与 sysFunction | 不建会扣 20 分 | Keil 能编译 |
| 2 | USART 命令框架 | 所有评分都靠串口触发 | 输入命令有响应 |
| 3 | OLED/LED 基础状态 | 上电初始化评分需要 | OLED idle、LED 可控 |
| 4 | RTC 设置与读取 | 后续文件名、日志、采样都依赖时间 | RTC Config / RTC now |
| 5 | Flash 参数模型 | ratio、limit、周期、log id 都要持久化 | config save/read |
| 6 | ADC 采样 | 系统核心数据来源 | 电位器变化能反映 |
| 7 | start/stop 采样状态机 | 主流程核心 | 串口和 KEY1 控制 |
| 8 | KEY 周期切换 | 采样周期评分项 | KEY2/3/4 切换 5/10/15s |
| 9 | OLED 采样显示 | 显示评分项 | hh:mm:ss 和 xx.xx V |
| 10 | 超限判断 LED2 | 超限评分项 | OverLimit + LED2 |
| 11 | TF 卡 sample/overLimit | 数据存储高分项 | 断电拔卡查文件 |
| 12 | conf 读取 config.ini | 配置件评分项 | 读取 Ch0 参数 |
| 13 | hide/unhide | 数据加密评分项 | HEX 编码正确 |
| 14 | hideData 存储 | 加密文件评分项 | 校验工具验证 |
| 15 | log 操作审计 | 单独高分项 | log0/log1/log2/log3 |
| 16 | test 系统自检 | 系统检测评分项 | 无卡/有卡两种结果 |
| 17 | 按评分流程全流程演练 | 防止局部可用但流程失败 | 完整录制前彩排 |

## 17. 调试顺序建议

每个模块都先写最小测试，不要直接混进大状态机。

推荐顺序：

```text
1. 串口 printf 测试
2. LED1/LED2 单独测试
3. KEY1/2/3/4 单独测试
4. OLED 显示两行字符串
5. RTC 设置和读取
6. Flash 读 ID
7. Flash 写入/读取参数
8. ADC 单次采样
9. TF 卡挂载
10. TF 卡创建文件夹
11. TF 卡写普通文本文件
12. 采样状态机整合
13. hide 编码整合
14. 日志整合
15. 按评分流程完整跑一遍
```

## 18. 关键风险点

### 18.1 LED 不一定直接接 MCU

用户 LED 区域需要把 H4 的 LED 选通端接到你选择的 MCU IO。比如代码用 PA4，则硬件上要接：

```text
PA4 -> H4 LED1
```

否则代码正确也不会亮。

### 18.2 limit 范围以前文和评分流程不一致

赛题前文写：

```text
0 - 500
```

评分流程写：

```text
0 - 200，并测试 300 非法
```

建议按评分流程实现 `0 - 200`。

### 18.3 RTC 没有电池会丢时间

题目说明：没有 CR2032 电池时，每次上电需要重新配置 RTC，不扣分。

### 18.4 log id 必须存在 Flash

不能只看 TF 卡里已有多少 log 文件。因为评分会清空 TF 卡，仍要求第五次上电生成 `log4.txt`。

### 18.5 hide 模式下 sample 不存储

hide 启用后：

```text
sample 不存普通数据
hideData 存隐藏数据
overLimit 仍然存普通超限数据
```

这个规则很容易漏。

## 19. 最小可交付版本路线

如果时间紧，按下面顺序抢分：

```text
1. 上电初始化 + 串口 + OLED idle
2. RTC Config / RTC now
3. test 自检
4. ratio / limit / config save/read
5. ADC + start/stop + OLED 显示
6. LED1 闪烁 + LED2 超限
7. TF 卡 sample/overLimit
8. hide/unhide
9. log 审计
10. conf 读取
```

优先级最高的是能贯通评分流程。日志、hideData、conf 可以最后补，但不要忘记它们分值也不低。

## 20. 每次提交建议

建议 Git 每完成一个阶段提交一次：

```text
init sysFunction structure
add usart command dispatcher
add rtc config command
add flash config storage
add adc sampling
add sampling state machine
add oled sample display
add tf card sample storage
add hide data encoder
add operation log storage
add self test command
```

这样出了问题容易回退，也方便和队友分工。

